to Run Hello_World.c
1. Make sure you have gcc compiler installed.(if you haven't done so, you can run "sudo apt-get install gcc" to get gcc compiler)
2. In your terminal, type:
    gcc Hello_World.c
3. Under the same directory, you should be able to see a file name **.out. e.g., 'a.out'
4. Under the directory, in terminal, type: ./a.out to get the output.
